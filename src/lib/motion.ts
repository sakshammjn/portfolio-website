/**
 * Shared motion easing.
 *
 * A gentle, premium ease-out (cubic-bezier) reused across the site so every
 * fade/rise lands with the same character. Consumed by <Reveal> and the Hero.
 */
export const easeOut = [0.22, 1, 0.36, 1] as const
